// src/components/HomeScreen.tsx
import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen = () => {
  return (
    <View>
      <Text>Home Screen</Text>
      {/* Adicionar conteúdo da tela inicial aqui */}
    </View>
  );
};

export default HomeScreen;
